export const CartItemSchema = {
  product_id: "",
  product_name: "",
  product_price: 0,
  product_image: "",
  quantity: 1,
  user_email: "" // Имейл на потребителя (незадължително за локална количка)
};

export const calculateTotal = (cartItems) => {
  return cartItems.reduce((total, item) => {
    return total + (item.product_price * item.quantity);
  }, 0);
};

export const mockCartItems = [
  {
    product_id: "prod_01",
    product_name: "Професионален комплект четки",
    product_price: 35.00,
    product_image: "https://images.unsplash.com",
    quantity: 1,
    user_email: "test@example.com"
  }
];

export const addToCartLocal = (currentCart, newProduct) => {
  const existingItem = currentCart.find(item => item.product_id === newProduct.product_id);
  
  if (existingItem) {
    return currentCart.map(item =>
      item.product_id === newProduct.product_id
        ? { ...item, quantity: item.quantity + 1 }
        : item
    );
  }
  
  return [...currentCart, { ...newProduct, quantity: 1 }];
};
